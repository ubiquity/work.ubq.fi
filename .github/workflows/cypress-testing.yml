name: Run Cypress testing suite
on:
  workflow_dispatch:
  pull_request:

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: 20.10.0
      - name: Checkout
        uses: actions/checkout@v4
      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          build: yarn run build
          start: yarn start
        env:
          NODE_ENV: "test"
          CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
          GITHUB_TOKEN: ${{ github.token }}
          # Values are set because the code requires them, but they are not actually used.
          SUPABASE_URL: "https://smsvjscodeuqtsdrgngf.supabase.co"
          SUPABASE_ANON_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InNtc3Zqc2NvZGV1cXRzZHJnbmdmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3MTgxMTU1MzksImV4cCI6MjAzMzY5MTUzOX0.tKtCSqn31NjDN6pD_URaBBWyjsepleC5cnegm89Uw60"
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: cypress-screenshots
          path: cypress/screenshots
          if-no-files-found: ignore
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: cypress-videos
          path: cypress/videos
          if-no-files-found: ignore
