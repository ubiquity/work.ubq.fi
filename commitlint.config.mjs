// Commitlint config for Deno-driven CLI
// Import the conventional preset via Deno's npm: spec
import configConventional from "npm:@commitlint/config-conventional";
export default configConventional;
